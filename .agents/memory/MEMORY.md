- [KaliGPT Stack](kaligpt-stack.md) — Main entry: `main.py` → Flask app at `pentest-lab/backend/app.py` on port 5000. Flask blueprints in `pentest-lab/backend/routes/`. FastAPI routers (tools/scanner/osint/reports) are SEPARATE from Flask and must NOT be imported into app.py.
- [CyberAI Full Features](cyberai-features.md) — 30 features across 6 layers implemented: AI fallback chain, server-side subscriptions (DB), Stripe, OSINT APIs, FAISS RAG, user auth, admin dashboard, notifications, support tickets, referrals, SAST scanner, PDF reports, CVE/NVD API, threat intel (OTX), Redis cache, PWA, onboarding tour, search API, analytics.
- [4D Engine Architecture](four-d-engine.md) — True 4D math in `artifacts/mr7-ai/src/lib/four-d-engine.ts`; TESSERACT_VERTICES/EDGES + project4Dto2D; Rotation4D class. Canvas components use `gpu-layer` CSS class.
- [Threat Intel API](threat-intel-api.md) — `/api/threat-intel/*` public GET routes in `artifacts/api-server/src/routes/threat-intel.ts`; engine singleton in `lib/threat-intelligence.ts`; registered before internalAuth in app.ts.
- [mr7-ai streaming fix](mr7-ai-streaming.md) — server always sends SSE regardless of `stream` flag; all API consumers must use SSE reader, never `resp.json()`
- [mr7-ai multi-provider AI](mr7-ai-multi-provider.md) — unified provider system supports OpenAI/Anthropic/Groq/Gemini/OpenRouter/Custom/Personal; "personal" is always available, URL editable from ProviderSettingsModal UI
- [mr7-ai cloud sync](mr7-ai-cloud-sync.md) — chats synced to PostgreSQL `cloud_chats` table via deviceId; GET/POST `/api/cloud-chats`; debounced 3s push from StoreProvider
- [mr7-ai personal-api migration](mr7-ai-personal-api.md) — all Replit OpenAI SDK calls replaced with getPersonalOpenAI()/callOnce() from ai-providers.ts; model "gpt-5.4" replaced with PERSONAL_DEFAULT_MODEL; max_completion_tokens → max_tokens
- [mr7-ai provider-freeze-fix](mr7-ai-provider-freeze.md) — personal provider URL was dead lhr.life tunnel; fix: use OPENAI_API_KEY env (Replit integration) as fallback, https://api.openai.com/v1 as base URL, 60s AbortController timeout on all streamCompletion calls
- [mr7-ai qr-sync](mr7-ai-qr-sync.md) — QR sync: push chats via POST /api/cloud-chats?deviceId, share URL ?sync=deviceId, auto-import on load; QrCode button in Sidebar bottom bar; IMPORT_CHATS action merges without duplicates
- [mr7-ai api-zod-preexisting](mr7-ai-api-zod.md) — api-zod must be built via `tsc --build` in lib/api-zod before api-server typecheck passes; no build script, use node_modules/.bin/tsc directly
- [mr7-ai zip-integration](mr7-ai-zip-integration.md) — ZIP added: migrations/, seeds/, docker-compose.yml/production, hf-spaces/, server/ (Replit integrations with audio/auth/batch/chat/image), tests/, vitest, swagger, tierRateLimit, collabRouter, subscriptionsRouter; server/ uses ./shared/schema not @shared/schema; callOnce() takes (msgs, maxTokens:number) NOT object; streamCompletion needs (provider,model,msgs) 3-5 args
- [mr7-ai godmode-modes](mr7-ai-godmode.md) — GodmodeConfig.mode type must stay in sync across GodmodeSettingsModal.tsx, chat-client.ts, store.tsx; 14 modes total (classic/ultraplinian/reason/hunter/agent/extended/maxoverdrive/unbound/jioreason/mythos/ultimate/think/max/abliterated)
- [mr7-ai catalog-freeze](mr7-ai-catalog-freeze.md) — ProviderSettingsModal catalog froze due to motion.div layout prop on 500+ items; fix: remove layout, paginate to 50/page; also const/let TDZ in canvas components must declare vars before resize() call
- [mr7-ai provider-key-routing](mr7-ai-provider-key-routing.md) — non-personal provider keys stored under localStorage key `mr7-ai-p-key-{id}`, URLs under `mr7-ai-p-url-{id}`; ChatView must read these per active provider and send as apiKey/apiBaseURL in every chat request
- [mr7-ai canvas-ctx-closure](mr7-ai-canvas-ctx.md) — TypeScript loses null-narrowing on canvas ctx inside RAF closures; fix: use `canvas.getContext("2d")!` (non-null assertion) like FuturisticBackground3D does; `if (!ctx) return; const c = ctx;` pattern still errors in closures
- [mr7-ai active-model-key](mr7-ai-active-model.md) — active model name lives at `state.activeModel` (top-level), NOT inside `state.settings`; dispatched via SET_MODEL action
- [mr7-ai drag fix](mr7-ai-drag-fix.md) — React 18 batches setState in native mousemove listeners; fix: `useDraggable` hook at `src/hooks/useDraggable.ts` mutates DOM directly during drag, setPos only on mouseup
- [mr7-ai canvas-color-hex](mr7-ai-canvas-color-hex.md) — 3-digit hex colors (e.g. `"#444"`) used in canvas gradient addColorStop with string concatenation (color+"44") produce invalid 5-digit hex and crash at runtime; always use 6-digit hex in node/color definitions
- [mr7-ai virtual-catalog](mr7-ai-virtual-catalog.md) — catalog uses WORLD_MODELS (curated) + generateVirtualCatalog() (generated); ALL_MODELS = combined; WorldModel.category union has 40+ values; tab type in ProviderSettingsModal must include "health" in the useState union
- [mr7-ai streaming-throttle](mr7-ai-streaming-throttle.md) — onChunk in ChatView must throttle PATCH_MSG to ≤48ms intervals via flushTimerRef; flush pending buffer in finally block before setStreaming(false) to avoid lost trailing content
- [mr7-ai local-proxy](mr7-ai-local-proxy.md) — Ollama/LM Studio/Jan must go via /api/local-proxy/* (server-side); direct browser→localhost fetch fails on Replit (HTTPS→HTTP mixed content + CORS); use streamLocalChatViaProxy not streamLocalChat
- [mr7-ai modal-reducer](mr7-ai-modal-reducer.md) — 130+ boolean modal states replaced by single useReducer(modalReducer); 'sidebar' MUST be in MODAL_IDS; SystemMasterHUD3D uses HUDPanel type not onClose; all modals lazy() with .then(m=>({default:m.X})) pattern
- [pentest-lab architecture](pentest-lab-arch.md) — Flask+SQLite platform at port 8000; 13 SSE tool endpoints all at /api/tools/<name>/stream; API keys DB-backed via ProviderKey model; _get_key() checks env→DB→alias; demo fallback streams knowledge base when no providers configured
- [mr7-ai topbar-draggable-windows](mr7-ai-topbar-draggable.md) — TopBar's 4 AI panels (ProviderHealthBadge3D, AIQuickSetupButton, QuantumPersona3D, PersonaSwitcher3D) use useDraggable with keys mr7-health-win/mr7-setup-win/mr7-qp3d-win/mr7-ps3d-win; backdrop overlays removed; header divs have cursor-move + onMouseDown drag handler
- [mr7-ai topbar-full-upgrade](mr7-ai-topbar-upgrade.md) — TopBar fully rewritten with 5 colored pill groups (Tools/cyan, Agents/purple, Intel/green, HUD/red, More/amber); HoloNotificationProvider always-mounted at top of JSX; Sidebar destructuring MUST include onOpenOsintHub (was missing, caused ReferenceError); AmbientLayer+FuturisticBackground3D+UltraHUD+SystemStatusWidget now rendered in App.tsx
- [mr7-ai chain-builder-3d](mr7-ai-chain-builder-3d.md) — ChainBuilderTab in ArsenalHubModal.tsx replaced with SVG visual node graph; GRAPH_NODES const array drives layout; particlePhase.current drives RAF-animated bezier particle flow; right-click drag on node creates new rule; useDraggable hook NOT used here (SVG internal drag)
- [mr7-ai ollama-hub](mr7-ai-ollama-hub.md) — Ollama v0.30.10 running on CPU; OLLAMA_LIBRARY_PATH required; launch via scripts/launch-ollama.py (Python detach); qwen2.5:0.5b inference confirmed; workflow auto-starts Ollama
- [mr7-ai local-engine-suite](mr7-ai-local-engine-suite.md) — 7 engines: Ollama(11434)/LMStudio(1234)/Jan(1337)/GPT4All(4891)/OpenWebUI(3000)/Llamafile(8081)/KoboldCPP(5001); /api/lb/* routes (health/chat/race/benchmark); WindowTray at bottom; MultiModelRaceModal+LocalBenchmarkModal in MODAL_IDS; new Sidebar props: onOpenMultiModelRace, onOpenLocalBenchmark
- [mr7-ai security-layer](mr7-ai-security.md) — CSRF token at GET /api/csrf-token (session-stored 64-char hex, timing-safe compare); sanitizeInputs middleware strips null bytes + control chars + limits to 512K chars; CSP via helmet; DOMPurify in SearchModal highlight(); api-server streamCompletion(provider,model,msgs,temp,opts) requires all 3-5 args; agent4 uses streamBest() wrapper that auto-picks best available provider
- [mr7-ai ts-route-fix](mr7-ai-ts-route-fix.md) — Express 5 async handlers typed as Promise<void>; early exits must be `res.end(); return;` NOT `return res.end()` (Response not assignable to void); inline `{ ...; return res.end(); }` must be split; TS2367 on impossible EngineId comparisons fixed with `(id as string) === "openwebui"`
- [mr7-ai feature-additions](mr7-ai-feature-additions.md) — Redis/email/OAuth/S3/pgvector/Sentry/OTel/webhooks/backup/feature-flags all added; @opentelemetry/* must be installed as direct deps (peer of @sentry/node); Express 5 rejects `:param(*)` wildcard patterns (use `:param` only); connect-redis@9 needs `redis` package not ioredis (skip Redis sessions, keep PG sessions)
- [mr7-ai mux-ws](mr7-ai-mux-ws.md) — /api/mux WS endpoint in api-server proxies HTTP via 127.0.0.1; client scheduleReconnect() must limit to RECONNECT_DELAY_MS.length attempts then useFallback(); else infinite loop
- [mr7-ai sw-cache](mr7-ai-sw-cache.md) — SW `kaligpt-v3` cached ALL GET responses (incl. compiled JS). HMR edits never reached browser. Fix: bump CACHE_NAME, exclude /src/ /node_modules/ /@ and URL params ?t= ?v= from cache.
- [mr7-ai sidebar-props](mr7-ai-sidebar-props.md) — Sidebar props must be in BOTH interface AND destructuring; missing from destructuring → ReferenceError at runtime (not undefined, actual ReferenceError). New props: onOpenAptIntel, onOpenNexusPanel.
- [OMNIX ABSOLUTE system](omnix-system.md) — 7-subsystem AI control layer built on top of Nexus. Ctrl+Shift+Z=panel, Ctrl+Shift+V=voice, Ctrl+Shift+Z from badge=palette. pnpm must stay at v9.15.4 (Node.js v20 incompatible with pnpm v10+).
- [mr7-ai worm-tools integration](mr7-ai-worm-tools.md) — OmnixAbsolute.ts already had all 9 systems; 4 key integration points were missing and added: processResponse in all AI paths, <omnix-commands> format in system prompt, 14 component registrations, OMNIX button in Sidebar
- [mr7-ai bulk modal restoration](mr7-ai-modal-restoration.md) — restoring many orphaned modals via scripted edits: never splice a JSX comment inside an element's attribute list (syntax error); not all modals share open/onOpenChange prop shape, verify each via tsc; app has large pre-existing unrelated tsc backlog, don't chase it.
- [mr7-ai deploy & workflow quirks](mr7-ai-deploy-and-workflows.md) — vite `preview.proxy` must duplicate `server.proxy` for prod `/api` routing; complete_project_import can spawn shadow `artifacts/...` workflows that conflict on ports with the real `.replit`-named ones — trust `.replit` as source of truth.
- [mr7-ai perf-theater modules](mr7-ai-perf-theater.md) — 11 dead perf-theater imports REMOVED from main.tsx; only networkResilience/smartCache/circuitBreaker kept. verify via grep before disabling any "systemX.init()" call.
- [mr7-ai perf optimizations](mr7-ai-perf-optimizations.md) — DPR cap rule (never *2 capped at 4), IntersectionObserver pattern for canvas, store localStorage debounce 300ms, polling 30s→120s+hidden-tab guard, React.memo for HUDBtn/VDivider, SYS/NEXUS/OMNIX as TopBar dropdowns at top:48px.
- [mr7-ai local-engine-window](mr7-ai-local-engine-window.md) — LocalAIWindow.tsx: launchEngine now streams SSE to launchLog/launchStatus; pullPct is separate state (not regex-parsed from pullLog); delete model via DELETE /api/local-engines/model/:model; benchmark via POST /api/local-engines/benchmark SSE; Ollama install progress uses spawn+curl -#; vite manualChunks must NOT include @monaco-editor/react (excluded from optimizeDeps).
- [mr7-ai workflows](mr7-ai-workflows.md) — Two workflows: "Start application" (Flask, port 5000, webview) and "mr7-ai API Server" (Express, port 8080, console). "mr7-ai Frontend" workflow (Vite, port 5173) added but NOT auto-started. API server auto-builds+starts via `pnpm run dev`. Ollama auto-launched by api-server on startup.
- [mr7-ai planet-orb-shared](mr7-ai-planet-orb.md) — 4 TopBar icon orbs (health/setup/persona/switcher) now delegate to shared `PlanetOrb.tsx` component for a consistent single-hue glowing-sphere design; avoid line-range python-splice replacements without checking for a leftover trailing `}` at the old block's end.
- [mr7-ai boot-screen-verification](mr7-ai-boot-screen.md) — screenshot tool does a fresh page load each call, and BootScreen.tsx's intro animation takes ~8-12s before dismissing; don't mistake repeated screenshots showing the boot animation as a stuck/broken app — wait or verify via typecheck/code reading instead.
- [mr7-ai orb color customization](mr7-ai-orb-customization.md) — user-overridable orb colors read from `state.settings.orbColors`; only override the color fed into PlanetOrb, keep surrounding status-based glow/border logic intact.
- [mr7-ai floating-panel-portal-rule](mr7-ai-floating-panel-portal.md) — any TopBar floating/draggable panel MUST render via `createPortal(..., document.body)`, not as a plain nested div, or it gets invisibly clipped by the header's backdrop-filter + overflow-hidden row. AIQuickSetupButton ✅ fixed. ProviderHealthBadge3D, QuantumPersona3D, PersonaSwitcher3D all fixed with createPortal in their AnimatePresence blocks.
- [mr7-ai perf audit](mr7-ai-perf-audit.md) — SSE+compression conflict rule, Vite chunk grouping pitfall, OSINT cache pattern, DB pool, WS heartbeat, LRU fix, SQL aggregation, QueryClient defaults.
