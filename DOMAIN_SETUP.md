# ربط دومين حقيقي بالمشروع (مثال: mr7.ai)

دليل خطوة بخطوة لشخص غير متخصص تقنيًا. اتبع الترتيب بالضبط.

## قبل ما تبدأ
- لازم يكون عندك الدومين مسجّل عند أي مسجّل دومينات (Namecheap، GoDaddy، إلخ).
- المشروع لازم يكون منشور (Published) على Replit مرة واحدة على الأقل.

---

## الخطوة 1 — أضف الدومين بإعدادات Replit Deployments

1. افتح مشروعك على Replit.
2. اضغط زر **Publish** (بالأعلى) — لو ما نشرته من قبل، انشره أول مرة بالإعدادات الافتراضية.
3. بعد النشر، افتح تبويب **Deployments**.
4. روح لقسم **Settings** داخل الـ Deployment، ثم **Custom Domain** (أو "Link a domain").
5. اكتب الدومين اللي تبيه (مثلاً `mr7.ai` أو `www.mr7.ai`).
6. Replit بيعطيك سجلات DNS تحتاج تضيفها عند مسجّل الدومين — احتفظ بهذي الصفحة مفتوحة، بتحتاجها بالخطوة الجاية.

## الخطوة 2 — أضف سجلات DNS عند مسجّل الدومين

روح لوحة تحكم الدومين عندك (Namecheap / GoDaddy / أي مزوّد)، لقسم **DNS Management** أو **DNS Records**، وأضف بالضبط اللي أعطاك ياه Replit بالخطوة السابقة. الشكل العام:

| نوع السجل | الاسم (Host) | القيمة (Value) | ملاحظات |
|---|---|---|---|
| **A** | `@` (يعني الدومين الرئيسي، مثل `mr7.ai`) | عنوان IP اللي يعطيك ياه Replit | يُستخدم لو تربط الدومين الجذري |
| **CNAME** | `www` | العنوان اللي يعطيك ياه Replit (مثل `xxxx.replit.app` أو مشابه) | يُستخدم لو تربط `www.mr7.ai` |
| **TXT** (لو طلبه Replit) | حسب تعليمات الصفحة | قيمة التحقق (verification) | يستخدم للتأكد إنك مالك الدومين فعليًا |

> **مهم:** انسخ القيم بالضبط من صفحة Replit — لا تخمّنها. كل مزود دومينات (Cloudflare, GoDaddy...) القيم تختلف باختلاف حسابك.

عادة تغييرات DNS تاخذ من دقائق لعدة ساعات عشان تنتشر عالميًا (Propagation) — هذا طبيعي.

## الخطوة 3 — تفعيل SSL (شهادة الأمان https)

لا تحتاج تسوي شي يدويًا هنا. بعد ما تنتشر سجلات DNS، Replit يصدر شهادة SSL تلقائيًا للدومين (عادة تاخذ من دقائق إلى ساعة). بترجع لصفحة Custom Domain وتشوف حالته يتحول من "Pending" إلى "Active" ويظهر القفل 🔒 بجانب الرابط.

لو ضل الحالة "Pending" لأكثر من ساعتين، تأكد إن سجلات الـ DNS مضبوطة صح بالضبط زي الخطوة 2 (أكثر سبب شائع للتأخير).

---

## (اختياري) الخطوة 4 — إضافة Cloudflare كطبقة CDN فوق الدومين

لو تبي تستفيد من تسريع Cloudflare وحماية إضافية، اتبع هذا الترتيب **بالضبط** — لا تبدأ بتفعيل Cloudflare قبل ما يشتغل الدومين مباشرة مع Replit أول:

1. **أول تأكد إن الدومين شغّال ومربوط بـ Replit مباشرة** (الخطوات 1-3 فوق) ويطلع "Active" — لا تتخطى هذي الخطوة.
2. سجّل حساب على [cloudflare.com](https://cloudflare.com) وأضف الدومين عندهم (زر "Add a Site").
3. Cloudflare بيقرأ سجلات DNS الحالية تلقائيًا ويعرضها لك — تأكد إن نفس سجلات الخطوة 2 (A و/أو CNAME اللي أعطاك ياها Replit) موجودة ومطابقة، **لكن خلّي وضع الـ Proxy مطفي (DNS only / السحابة الرمادية)** بهالمرحلة.
4. غيّر Nameservers عند مسجّل الدومين إلى الاثنين اللي يعطيك ياهم Cloudflare (يظهرون بخطوة الإعداد عندهم).
5. انتظر لين Cloudflare يأكد إن الدومين نشط عندهم (يوصلك إيميل أو يتحول الحالة لـ "Active" بلوحة التحكم).
6. **الحين بس** فعّل وضع الـ Proxy (السحابة البرتقالية 🟠) على سجلات الدومين — هذا يفعّل الـ CDN والحماية.
7. اضبط قواعد الـ caching والضغط التلقائي للصور من لوحة تحكم Cloudflare (Speed → Optimization، و Caching → Configuration) — هذا الجزء موثّق بتفصيل أكثر ضمن ملف `CDN_SETUP.md` المنفصل إذا كان أو حين يُضاف لاحقًا ضمن خطة تحسينات التوسّع.

> ترتيب مهم: DNS مباشر مع Replit أول ← يشتغل ← بعدين تحوّل لـ Cloudflare ← تتأكد يشتغل بدون Proxy ← بعدين تفعّل الـ Proxy. لو قلبت الترتيب، ممكن الموقع يوقف مؤقتًا لين تصحح الإعدادات.

---

## استكشاف الأخطاء الشائعة

- **"Domain verification failed"**: تأكد السجل TXT أو CNAME منسوخ بالضبط بدون مسافات زايدة.
- **الموقع يفتح بس بدون SSL (تحذير "Not Secure")**: انتظر شوي أكثر، أو تأكد إن سجلات DNS ما تغيرت بعد ما فعّلتها.
- **الدومين يشتغل بس Cloudflare يعطي خطأ 522/525**: رجّع وضع الـ Proxy لـ "DNS only" مؤقتًا وتأكد الدومين يشتغل مباشر مع Replit، بعدين فعّل الـ Proxy من جديد.
