اقرأ الكود الموجود أولاً. ممنوع حذف أو تعديل أي كود موجود — فقط أضف كود جديد فوق البنية الحالية (skillsEngine.ts, filesEngine.ts, ArsenalHubModal.tsx).

الهدف: بناء نظام Tool-Calling ديناميكي وشامل ذكي ومترابط، يسمح للذكاء الاصطناعي (عبر Anthropic API tool_use) بالتحكم الكامل، والاستخدام الفعلي، والتشغيل العملي لكافة موديولز ووظائف Arsenal Hub، وعناصر القوائم الجانبية (Sidebar) والعلوية (Top Bar)، وجميع الأنظمة والميزات والمكونات البرمجية القائمة في المشروع بشكل مطلق. لا يقتصر النظام على مجرد "فتح" أو استدعاء الواجهات، بل يمتد لتمكين النموذج من استخلاص مهاراتها، والعمل عليها عملياً، والدمج بين عدة أدوات وأنظمة معاً في نفس الوقت (Multi-Tool Orchestration) واختيار المزيج الأنسب منها تلقائياً بناءً على طلب المستخدم وسياق المحادثة لتنفيذ المهام المركبة.

1) ملف جديد src/lib/toolsRegistry.ts:
   - بناء سجل ديناميكي وتفاعلي يستوعب ويغطي جميع الأدوات والموديولز الموجودة في Arsenal Hub (كافة الوحدات بدون تخطي أي منها).
   - تضمين كافة العناصر والوظائف والأزرار التفاعلية الموجودة في القائمة الجانبية (Sidebar) والشريط العلوي (Top Bar) ضمن السجل كأدوات قابلة للاستدعاء والتحكم والتشغيل من قبل الذكاء الاصطناعي (مثل التنقل، تغيير الإعدادات، فتح النوافذ، وعرض الحالات).
   - توسيع السجل ليشمل ويستوعب بالكامل جميع الأنظمة والميزات والملفات والمكونات البرمجية الأخرى المتوفرة في سياق المشروع دون استثناء أي ميزة أو نظام داخلي، وتحويل قدراتها البرمجية والتشغيلية إلى أدوات معرفة بدقة تسمح بالتلاعب بالبيانات وقراءة الحالات الحية وتنفيذ الأوامر.
   - تعريف النوع ليدعم التشغيل الفعلي المتعدد:
     type ToolDefinition = {
       moduleId: string;
       name: string;
       description: string;
       inputSchema: any; // JSON schema متوافق مع صيغة Anthropic tools param
       execute: (input: any) => Promise<any>;
     }
   - دوال الإدارة: registerTool(def), getRegisteredTools(), و getAnthropicToolsParam() الذي يقوم بتحويل وتجهيز كافة الأدوات المسجلة تلقائياً إلى الصيغة المعتمدة لـ Anthropic API.

2) ربط موديولز الـ Arsenal Hub والـ Navigation UI وكافة أنظمة المشروع للتشغيل الفعلي:
   - لجميع الموديولز المتوفرة في المنصة، بالإضافة إلى عناصر الـ Sidebar والـ Top Bar، وكافة الأنظمة والميزات الفرعية والأساسية في الكود: قم بإنشاء ملفات التنفيذ أو الربط اللازمة في src/lib/tools/ لتغطية كافة الوظائف. يجب أن تتيح دالة execute() للنموذج استخدام الميزة عملياً (مثل إرسال مدخلات، استقبال مخرجات ومعالجتها، تمرير البيانات من نظام إلى آخر) بناءً على الـ states والدوال المعرفة في المشروع، وتسجيلها مباشرة في السجل العام.

3) الدمج في src/lib/chat-client.ts (إضافة فقط، بدون حذف) ودعم حلقة الأدوات المتعددة:
   - أضف باراميتر اختياري enableTools?: boolean لدالة streamChat الحالية.
   - إذا تم تفعيله (true)، قم بإرسال مصفوفة الأدوات كاملة عبر getAnthropicToolsParam() مع الطلب الصادر إلى Anthropic API.
   - معالجة دورة حياة الـ tool_use المتقدمة: تمكين النموذج من استدعاء أدوات متعددة بالتوازي أو بالتتابع (Chain of Thought / Multi-tool calling). عند استقبال طلب استدعاء أداة أو عدة أدوات، يتم تشغيل دالة execute() المطابقة لكل منها فوراً، وربط مخرجات أداة لتعمل كمدخلات لأداة أخرى إذا لزم الأمر، وإرجاع كافة الـ tool_results في نفس سياق المحادثة ليستمر النموذج في التفكير والعمل حتى يختار الأداة النهائية المناسبة للطلب ويصل للرد النهائي.

4) تحديث واجهة المستخدم src/components/ChatView.tsx:
   - أضف مفتاح تحكم وتفعيل (Toggle) جديد باسم "Enable Tools" في قائمة الإعدادات الحالية دون المساس بالخيارات الأخرى.
   - عند التفعيل، يتم تمرير enableTools: true إلى دالة المحادثة.
   - عرض مؤشر مرئي ديناميكي في الواجهة أثناء تشغيل الأدوات يحمل شارة تتبع حية "🔧 Using: [name]" متوافق مع نمط شارات الـ Skills الحالية، ويظهر الأدوات النشطة التي تعمل معاً حالياً.

5) التكامل مع محرك المهارات skillsEngine.ts وتوليد المهارات المركبة:
   - تمكين أي مهارة (سواء كانت يدوية، منشأة بالذكاء الاصطناعي، أو مرفوعة) من استدعاء وتضمين مهارات الأدوات المتاحة في سجل Arsenal Hub والـ UI (Sidebar/Top Bar) وكافة أنظمة المشروع المتكاملة ضمن توجيهاتها، بحيث يستطيع النظام مزج مهارات هذه الأدوات والأنظمة معاً لإنشاء "سير عمل ذكي" يختار فيه الذكاء الاصطناعي تلقائياً الميزة الأنسب للخطوة الحالية بناءً على مجريات المحادثة.

ملاحظة هامة للمطور: تأكد من أن النظام مرن ومكتمل البنية البرمجية، ويمنح النموذج سيطرة وتحكماً برمجياً كاملاً وتشغيلياً على واجهات الخلفية والأزرار التفاعلية والموديولز وعناصر الـ Sidebar والـ Top Bar وجميع ميزات وأنظمة المشروع بشكل مباشر، لضمان عمل الأدوات كمنظومة مهارات متكاملة تتفاعل وتتبادل البيانات وتنفذ المهام عملياً وليس مجرد نوافذ للعرض.
